package com.rootdown.dev.adidevibm.lib.abstracts

import androidx.lifecycle.ViewModel

abstract class ViewModelBase {
    // handle data
    abstract fun bObserv ()

    // communication activity keys per
    // fragment comm fragment via activity keys
    abstract fun keyI ()
}